"""
Stepik Tasks — Math Module
Date: 10 July 2026
Topics: sqrt(), pow(), floor(), ceil(), pi, tan(), sin(), cos(), radians()
Result: 7 tasks completed

Notable solutions:
- Task #7: Quadratic equation with discriminant (two peppers 🌶️️)
- Task #6: Four different mean values (arithmetic, geometric, harmonic, quadratic)
- Task #5: Regular polygon area using trigonometric formula
- Task #4: Trigonometric expression with double-angle formula for tan²

Personal note: Solved all tasks independently without looking at comments!
"""

  # Задача №7(Задача с 2 перчиками).  Квадратное уравнение 🌶️🌶️. Даны три действительных числа a, b, c. Напишите программу, которая находит действительные корни квадратного уравнения:
  # ax^2+bx+c=0
  # Если уравнение имеет один корень, то нужно вывести его, а если уравнение имеет два корня, то следует вывести их в порядке возрастания, каждый на отдельной строке. При этом если уравнение не имеет действительных корней, то программа должна вывести текст «Нет корней» (без кавычек).
  # На вход программе подаются три действительных числа a≠0,b,c, каждое на отдельной строке.
  # Программа должна вывести действительные числа – корни уравнения, – каждый на отдельной строке, в порядке возрастания, если они существуют, или текст «Нет корней» (без кавычек) в противном случае.
a, b, c = float(input()), float(input()), float(input())
from math import sqrt, pow
D = pow(b, 2) - (4 * a * c)
if D < 0:
    print("Нет корней")
elif D == 0:
    print((-b) / (2 * a))
elif D > 0:
    print(min(((-b) - sqrt(D)) / (2 * a), ((-b) + sqrt(D)) / (2 * a)), max(((-b) - sqrt(D)) / (2 * a), ((-b) + sqrt(D)) / (2 * a)), sep = "\n")

  # Задача №6. Средние значения. В математике выделяют следующие средние значения:
  # среднее арифметическое чисел a и b
  # среднее геометрическое чисел a и b
  # среднее гармоническое чисел a и b
  # среднее квадратичное чисел a и b
  # На вход программе подаются два положительных действительных числа a и b​, каждое на отдельной строке.
  # Программа должна вывести 4 числа (каждое на отдельной строке) – среднее арифметическое, геометрическое, гармоническое и квадратичное.
a, b = float(input()), float(input())
from math import sqrt, pow
print((a + b) / 2, sqrt(a * b), (2 * (a * b)) / (a + b), sqrt((pow(a, 2) + pow(b, 2)) / 2), sep = "\n")

  # Задача №5. Правильный многоугольник 🔶.Правильный многоугольник — выпуклый многоугольник, у которого равны все стороны и все углы между смежными сторонами. Площадь правильного многоугольника с длиной стороны a и количеством сторон n вычисляется по формуле:
  # S=(n⋅a^2)/4tg(π/n)
  # Даны два числа: натуральное число n и действительное число a. Напишите программу, которая находит площадь указанного правильного многоугольника. ​
n, a = float(input()), float(input())
from math import pi, pow, tan
print((n * pow(a, 2)) / (4 * (tan(pi / n))))

  # Задача №4. Тригонометрическое выражение. Напишите программу, вычисляющую значение тригонометрического выражения:
  # sinx+cosx+tg^2x  по заданному числу градусов x.
  # На вход программе подаётся одно действительное число x, измеряемое в градусах​.
  # Программа должна вывести одно число – значение тригонометрического выражения.
x = float(input())
from math import radians, sin, cos
c = radians(x)
f = (1 - cos(2 * c)) / (1 + cos(2 * c))  # замена формулы tg^2x на выражение через двойной угол.
print(sin(c) + cos(c) + f)

  # Задача №3 - Евклидово расстояние 📏.На плоскости евклидово расстояние ρ между двумя точками (x1;y1) и (x2;y2) определяется так:
  # ρ=sqrt(x1−x2)^2+(y1−y2)^2
  # Напишите программу, определяющую евклидово расстояние между двумя точками, координаты которых заданы.
x1, y1, x2, y2 = float(input()), float(input()), float(input()), float(input())
from math import sqrt, pow
print(sqrt(pow((x1 - x2), 2) + pow((y1 - y2), 2)))
 
  # Задача №2 - Пол и потолок.Напишите программу, которая принимает на вход действительное число x и вычисляет по нему значение: ⌊x⌋+⌈x⌉
  # На вход программе подаётся одно действительное число x.
  # Программа должна вывести одно число – значение указанного выражения.
  # Примечание 1.⌊x⌋ – пол числа (округление x до ближайшего целого в меньшую сторону), ⌈x⌉ – потолок числа (округление x до ближайшего целого в большую сторону).
x = float(input())
from math import floor, ceil
print(floor(x) + ceil(x))

  # Задача №1 - Площадь и длина. Напишите программу, определяющую площадь круга и длину окружности по заданному радиусу R.
  # На вход программе подаётся одно действительное число R​.
  # Программа должна вывести два числа (каждое на отдельной строке) – площадь круга и длину окружности радиуса R.
  # Примечание. Используйте константу pi из модуля math. 
r = float(input())
from math import pow, pi
print(pi * pow(r, 2))
print(2 * pi * r)
