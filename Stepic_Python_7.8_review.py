"""
Stepik: Python Programming
Topic: Code Review — Debugging, Logic Optimization, and Edge Cases
Date: 04 August 2026
Status: Completed ✅

Tasks solved: 8 (Ревью кода 1-6, Очень странные никнеймы, Цифровые сообщения)
Topics covered:
- Identifying and fixing syntax/logical errors in pre-written code.
- Loop control and flow (while True, break, correct counter placement).
- Mathematical optimization (e.g., extracting the first digit via string length or powers of 10).
- Edge case handling (e.g., initializing max negative values, handling "NO" outputs).
- String manipulation and conditional logic (e.g., algorithmic nickname generation).

Notable challenges:
- Task #7 (Цифровые сообщения): Fixed an off-by-one error by moving the `total` counter *before* the `break` condition, ensuring the final terminating message is correctly counted.
- Task #8 (Ревью кода-6 🌶️🌶️): Properly initialized the maximum negative number (`max_num = -10**6`) for clean integer math, and elegantly used `total < 0` to verify the existence of negative numbers.
- Task #2: Explored the balance between strict line-by-line correction (as sometimes required by autograders) and elegant algorithmic optimization.

Personal note: Solved these during a heatwave in Minsk. Learning to balance strict debugging with self-care, and recognizing that catching a tricky logical trap (like the off-by-one error) is a huge win. Python logic is a great anchor!
"""
  # Задача №1 - Ревью кода-1.
  # На обработку поступает натуральное число. Нужно написать программу, которая выводит на экран произведение цифр введённого числа. Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 3). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Код задания:
n = input()
product = n % 10
while n >= 10:
    digit = n % 10
    product = product * digit
    n //= 10
print(product)

  # Мой код:
n = int(input())
total = 1
while n != 0:
    digit = n % 10
    total *= digit
    n //= 10
print(total)

  # Задача №2 - Ревью кода-2.
  # На обработку поступает натуральное число. Нужно написать программу, которая выводит на экран его первую (старшую) цифру. Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 2). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Код задания:
n = int(input())
while n > 0:
    n %= 10
print(n)

  # Мой код:
n = int(input())
z = len(str(n))      # Ведь задача ревью - это не только поиск ошибок, но и оптимизация кода. Чего перебирать цифры, когда можно сразу вычислить нужную.
print((n // 10 ** (z - 1)) % 10)

  # Задача №3 - Ревью кода-3.
  # На обработку поступает последовательность из 7 целых чисел (каждое на отдельной строке). Нужно написать программу, которая подсчитывает и выводит сумму всех чётных чисел последовательности. Если таких чисел нет, программа должна вывести 0. Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 4). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Код задачи:
s = 1
for i in range(1, 7):
    n = input()
    if i % 2 == 0:
        s = s + n
print(s)

  # Мой код:
total = 0
for _ in range(7):
    n = int(input())
    if n % 2 == 0:
        total += n
if total == 0:
    print(0)
else:
    print(total)

  # Задача №4 - Ревью кода-4 🌶️.
  # На обработку поступает последовательность из 10 целых чисел (каждое на отдельной строке). Нужно написать программу, которая выводит на экран количество неотрицательных чисел последовательности и их произведение. Если неотрицательных чисел нет, требуется вывести на экран «NO» (без кавычек). Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 4). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Примечание. При необходимости вы можете добавить необходимые строки кода.
  # Код задания:
count = 0
p = 0
for i in range(1, 10):
    x = int(input())
    if x > 0:
        p = p * x
        count = count + 1
if count > 0:
    print(x)
    print(p)
else:
    print('NO')

  # Мой код:
counter = 0
total = 1
for _ in range(10):
    x = int(input())
    if x >= 0:
        counter += 1
        total *= x
if counter > 0:
    print(counter, total, sep = "\n")
else:
    print("NO")

  # Задача №5 - Ревью кода-5 🌶️.
  # На обработку поступает натуральное число. Нужно написать программу, которая выводит на экран максимальную цифру числа, кратную 3. Если в числе нет цифр, кратных 3, требуется на экран вывести «NO» (без кавычек). Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 5). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Примечание 1. Число 0 делится на любое натуральное число.
  # Примечание 2. При необходимости вы можете добавить нужные строки кода.
  # Код задачи:
n = int(input())
max_digit = n % 10
while n > 0:
    digit = n % 10
    if digit % 3 == 0:
        if digit < max_digit:
            digit = max_digit
    n = n % 10
if max_digit == 0:
    print('NO')
else:
    print(max_digit)

  # Мой код:
n = int(input())
largest = 0
counter = 0
while n != 0:
    digit = n % 10
    if digit % 3 == 0 and digit >= largest:
        largest = digit
        counter += 1
    n //= 10
if counter == 0:
    print("NO")
else:
    print(largest)

  # Задача №6 - Очень странные никнеймы.
  # В свободное от модерации курсов время Сэм пишет спам-ботов на заказ. Для каждого бота он формирует никнейм по простому (но странному) алгоритму. Сначала Сэм вводит стартовую строку – произвольное имя. Дальше, пока длина строки меньше 10 символов, он повторяет правило:
  # если текущая длина строки кратна 4, прибавляет справа символ x
  # иначе, если текущая длина строки кратна 5, прибавляет справа символ y
  # в противном случае прибавляет слева символ z
  # В конце Сэм добавляет слева символ @ и завершает формирование никнейма.
  # Перед вами программа, которая частично реализует алгоритм Сэма, но содержит ошибки. Найдите и исправьте все ошибки, допущенные в этой программе (их ровно 5), так, чтобы она правильно формировала никнейм по алгоритму Сэма.
  # Код задачи:
s = input()

while len(s) <= 10:
    if len(s) % 4:
        s = s + 'x'
    elif len(s) // 5 == 0:
        s = s + 'y'
    else:
        s = s + 'z'

s = '&' + s
print(s)

  # Мой код:
s = input()
while len(s) < 10:
    if len(s) % 4 == 0:
        s += "x"
    elif len(s) % 5 == 0:
        s += "y"
    else:
        s = "z" + s
print("@" + s)

  # Задача №7 - Цифровые сообщения 📻.
  # Майк и Дастин обмениваются цифровыми сообщениями через приёмник. Каждое сообщение представляет собой целое число. Майк и Дастин договорились, что последнее сообщение будет оканчиваться на 11, после него переписка прекращается. Лукас перехватывает сообщения Майка и Дастина и хочет посчитать, у скольких из них длина более 7 символов. Для этого Лукас написал программу, однако допустил в ней несколько ошибок из-за того, что прогуливал уроки информатики в школе.
  # Исправьте программу Лукаса так, чтобы она корректно находила количество сообщений длиной более 7 символов и выводила текст в следующем виде:
  # n/m
  # где n – количество сообщений длиннее 7 символов, m – общее количество сообщений.
  # Примечание 1. Лукас не знает, сколько ошибок он допустил. ☹️
  # Примечание 2. Последнее сообщение также нужно учитывать при подсчёте сообщений длиной более 7 символов и общего количества сообщений.
  # Код задачи:
num = int(input())
cnt = 0
total = 0
while num % 100 != 11:  # в Stepic тут нет ":", я поставила, чтобы не было конфликта.
    if len(num) > 7:
        cnt += 1

    total =+ 1
    num = int(input())

print(cnt, '/', total, sep='')

  # Мой код:
num = int(input())
cnt = 0
total = 0
while True:
    total += 1  # Сначала считаем текущее сообщение!
    if len(str(num)) > 7:
        cnt += 1
    if num % 100 == 11:
        break
    num = int(input()) # Запрашиваем следующее только если не вышли из цикла
print(f"{cnt}/{total}")
    


  # Задача №8 - Ревью кода-6 🌶️🌶️.
  # На обработку поступает последовательность из 10 целых чисел (каждое на отдельной строке). Известно, что вводимые числа по абсолютной величине не превышают 10^6. Нужно написать программу, которая выводит на экран сумму всех отрицательных чисел последовательности и максимальное отрицательное число в последовательности. Если отрицательных чисел нет, требуется вывести на экран «NO» (без кавычек). Программист торопился и написал программу неправильно.
  # Найдите все ошибки в этой программе (их ровно 5). Известно, что каждая ошибка затрагивает только одну строку и может быть исправлена без изменения других строк.
  # Код задачи:
mx = 0
s = 0
for i in range(11):
    x = int(input())
    if x < 0:
        s = x
    if x > mx:
        mx = x
print(s)
print(mx)

  # Мой код:
max_num = -10 ** 6  # В данном случае максимальным числом должно быть самое маленькое. По условию задачи вводимое число не превышает по величине 10^6, оно нужно отрицательным.
total = 0
for _ in range(10):
    n = int(input())
    if n < 0:
        total += n
    if n < 0 and n >= max_num:
        max_num = n
if total < 0:
    print(total, max_num, sep = "\n")
else:
    print("NO")


